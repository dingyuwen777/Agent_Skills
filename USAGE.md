# AI 辅助开发使用说明

本说明介绍如何在 **Codex、Cursor、Claude Code、DeepSeek Harness** 等团队支持的 AI Agent 中完成日常开发任务。

以下内容适用于**已经完成项目开发环境配置**的项目。使用时，直接打开当前项目，并用自然语言说明：

- 你希望解决什么问题；
- 已知的需求、现象、报错或限制；
- 希望 AI 只分析、直接修改，还是完成到 PR。

项目结构、代码位置、当前依赖、接口、数据结构、测试入口和 CI 等能够自行确认的信息，应由 AI 根据当前项目实际情况自行调查。

### 授权文件

如果当前项目需要授权文件，请把维护者提供的 `license.lic` 放到：

```text
<project>/.agents/license.lic
```

不需要登录、联网激活或机器码。续期时直接用新的 `license.lic` 覆盖同一路径即可，不需要重新配置项目。

如果工具提示授权文件缺失、尚未生效、已过期或无效，请先确认文件路径和维护者提供的授权文件；不要修改文件内容，因为任何修改都会使签名校验失败。

---

## 1. 在不同 AI Agent 中怎么使用

不同工具的入口略有区别，但开始任务后的使用方式基本一致：**打开当前项目，直接描述真实目标。**

不需要提前整理所有技术细节，也不需要告诉 AI 应该先打开哪些代码文件。

### 1.1 Codex

在 Codex 中打开当前项目，然后直接描述任务。

例如：

```text
完成用户登录超时处理。

基于当前仓库真实实现进行调查和修改。
完成必要测试和文档同步后，提交 PR 给维护者审核，不要合并 main。
```

如果只是讨论方案：

```text
先不要修改代码。

基于当前仓库真实实现分析用户登录超时问题，并给出实施方案。
```

继续已有任务：

```text
继续当前任务，保持原来的目标、范围和已经确认的决定，完成到原来的交付终点。
```

### 1.2 Cursor

在 Cursor 中打开项目，在 Agent 模式中直接描述任务。

例如：

```text
完成订单导出功能。

先根据当前代码确认现有实现、真实接口和数据结构。
完成必要测试、文档同步和代码检查，最后提交 PR 给维护者审核。
```

如果已经知道大致相关模块，可以提供导航信息：

```text
重点从订单导出页面和当前订单接口开始检查，但请沿真实调用链确认完整影响范围。
```

不要因为已经选中了某个文件或某段代码，就默认问题只存在于该位置。

### 1.3 Claude Code

从当前项目根目录进入 Claude Code，然后直接描述任务。

例如：

```text
修复用户同步任务偶发重复写入的问题。

先复现或建立失败证据，再定位根因。
不要通过增加 retry、吞异常或临时去重掩盖问题。
完成修复和回归测试后提交 PR。
```

对于较大的任务：

```text
这个任务比较大：<目标>。

先恢复当前项目真实情况。
不要一开始假设完整实施路径，先确认已经明确的部分、真正的 blocker 和暂时未知项。
主要路径清晰后再形成实施计划并继续开发。
```

### 1.4 DeepSeek Harness（Windows）

Windows 项目中，直接双击项目根目录的：

```text
DeepSeek-Harness.cmd
```

从当前项目进入 DeepSeek Harness。

打开后直接描述开发任务即可，例如：

```text
基于当前项目真实代码完成这个功能：<需求>。

先确认现有实现和接口，完成必要测试和文档同步。
最终提交 PR 给维护者审核，不要合并主分支。
```

或者：

```text
修复这个 Bug：<现象 / 报错 / 复现方式>。

先确认当前真实实现和根因，修复后完成回归验证。
```

---

## 2. 30 秒开始一个任务

大多数日常任务不需要写很长的提示词。

### 新功能

```text
基于当前仓库真实实现完成这个功能：<需求>。

完成必要测试和文档同步，最后提交 PR 给维护者审核，不要合并 main。
```

### Bug

```text
修复这个 Bug：<现象 / 报错 / 复现步骤>。

先复现并定位根因，补必要回归测试，完成后提交 PR。
```

### 只讨论方案

```text
先不要修改代码。

基于当前仓库真实实现分析这个目标，并给出实施方案、主要取舍、影响和验证方式。
```

### 只做 Review

```text
只审查当前 PR，不修改代码。

从真实需求重新判断实现是否正确，并检查测试是否充分。
```

### 只做测试

```text
针对当前功能做独立功能测试和黑盒验收，只测试，不修改生产代码。
```

### 继续已有任务

```text
继续当前任务，保持原来的目标、范围和已经确认的决定，完成到原来的交付终点。
```

---

## 3. 先明确希望任务做到哪里

开始任务时，最好明确**交付终点**。

常用的有以下几种。

### 3.1 只分析，不修改

```text
只分析，不修改代码。
```

适合：

- 方案讨论；
- 架构审视；
- Bug 根因分析；
- 风险评估；
- Code Review；
- 资料核验。

### 3.2 完成修改，但不操作 Git

```text
完成代码修改和必要测试，但不要 commit、push 或创建 PR。
```

适合希望先自行检查修改结果的情况。

### 3.3 完成到 PR Ready

普通团队开发最常见：

```text
完成开发、测试、文档和开发侧复核，然后提交 PR 给维护者审核。

不要合并主分支。
```

一般情况下，普通开发者到 **PR Ready** 即可。

后续的最终审核、合并主分支、Release、Deploy 或生产数据操作，应由拥有相应权限的人或正式流程完成。

### 3.4 完成交付到 main

如果你本身具备维护权限，并希望完成整个交付流程，可以明确说：

```text
完成这个任务，并按当前项目流程交付到 main。
```

AI 仍然应该遵守当前项目真实存在的：

- Review；
- CI；
- 分支保护；
- 权限；
- 发布和部署约束。

不能为了完成任务而绕过这些门禁。

---

## 4. 一次正常开发任务应该怎么进行

正常研发任务的整体流程通常是：

```text
提出真实目标
    ↓
读取当前项目事实
    ↓
确认需求和影响范围
    ↓
必要时讨论方案 / 根因
    ↓
实现
    ↓
测试和验证
    ↓
检查文档影响
    ↓
Review
    ↓
提交 PR
    ↓
PR Ready
```

你不需要手工指挥每个步骤。

AI 应根据当前项目自行确认：

- 项目结构；
- 当前语言和框架；
- 当前依赖版本；
- 实际接口；
- 数据结构；
- 现有实现；
- 调用链；
- 已有测试；
- CI；
- 当前 Git 状态；
- 项目已有编码和设计方式。

如果当前事实已经能够从代码、配置、测试或工具确认，就不应该反复向你询问。

### 4.1 多 Agent 会自动判断，不需要手工指定

当前版本的项目安装二进制会自动完成多 Agent 所需的项目级配置：补齐项目工程规则入口，并为 **Codex、Claude Code、Cursor、DeepSeek Harness** 安装各自可发现的原生角色执行适配。日常开发者不需要手工创建角色文件，也不需要手工写 subagent 命令。

安装或升级后建议**新建一次宿主会话**再开始正式任务。尤其是 Claude Code：如果当前会话启动时项目里还没有角色目录，首次安装创建目录后，旧会话可能不会立即发现这些角色；重开会话即可。其他宿主也以当前会话实际可用能力为准。

对复杂开发任务，AI 会先判断拆分是否真的有收益，而不是固定启动多个 Agent：

- `NO_SPLIT`：任务局部、强顺序依赖或拆分成本更高，保持单 Agent；明显的小任务不会为了展示编排而单独刷一条 `NO_SPLIT` banner；
- `MAY_SPLIT`：存在独立工作，但收益不一定覆盖协调成本，由当前任务和宿主能力决定；
- `MUST_SPLIT`：存在真正独立、可验收且有明显并行、上下文隔离或独立复核价值的工作；当前宿主支持 subagent 时会自动拆分。

你不需要手工写 subagent 命令，也不需要自己指定“前端 Agent / 后端 Agent / 测试 Agent”。

实际拆分时，AI 应在开始前告诉你：

- 拆了几个 Agent；
- 每个 Agent 负责什么；
- 哪些只读、哪些允许修改；
- 哪些并行、哪些有前后依赖。

运行中只在关键完成、Finding、失败、阻塞或职责变化时更新，不会持续刷子 Agent 的底层日志。任务结束时会汇总实际 Agent 的工作和证据。

如果当前 Codex、Claude Code、Cursor、DeepSeek Harness 或其他宿主**没有可用的 subagent / delegation 能力**，即使任务属于 `MUST_SPLIT`，也会明确说明并**自动降级为单 Agent 继续正常执行**。不会仅因为宿主没有多 Agent 能力就中断一个本来能够完成的任务。

项目本身已经存在的 Review、CI、权限、安全或其他 required gate 仍然照常执行；单 Agent 降级只影响多 Agent 带来的额外收益，不会绕过这些门禁。

### 4.2 多 Agent 不会无限扩张

多 Agent 默认由 Main/Parent 统一编排：**同时活动的子 Agent 默认不超过 3 个**，child 默认不继续创建 child；同一工作区默认只允许一个写入者，确有隔离的 worktree/environment 才并行多个 Writer。

Parent 会把当前 revision 和已确认决定一起作为委派事实。子 Agent 基于旧 revision 或旧决定返回时会进入 `STALE_RESULT`，先重新验证再使用；同一 transient child 失败最多自动重试一次，再次同类失败进入 `STOP_CHILD_RETRY`，不会无限重启 Agent。证据已经充分或后台 Agent 已过期/无价值时，会停止等待，并在宿主支持时取消它。

Review 发现 `OUT_OF_SCOPE` 时默认只记录。只有通过 **Follow-up Admission Gate**，确认有真实证据、独立价值、不是重复事项且当前授权允许，才可能进入后续 Backlog；它**不会自动创建 Issue** / Change / Branch / PR / Agent，**不会自动执行**，也**不会递归派生**新的 Follow-up。

这些默认值以后不靠继续“感觉上再加规则”调整。需要优化拆分阈值或并发预算时，应使用**真实历史任务**比较耗时、Agent 数、返修轮数、CI 重跑和人工干预等证据；**不要继续凭感觉增加 Agent**，也不会为了此目的自动上传遥测数据。

---

## 5. 什么时候应该让 AI 自己处理，什么时候需要你决定

这是日常使用中最重要的边界之一。

### 通常应该由 AI 自己确认

例如：

- 代码在哪里；
- 当前使用什么框架；
- 当前依赖版本；
- 哪个测试命令存在；
- 当前接口实际返回什么；
- 当前数据库字段是什么；
- 某个函数是否还有调用方；
- 当前 CI 为什么失败；
- 项目已有编码规范是什么；
- 当前分支和 diff 是什么。

这些问题能从项目中找到答案，就应该先调查，而不是直接问人。

### 通常需要人决定

如果项目没有已有明确决定，以下问题通常需要业务 Owner、技术负责人或维护者确认：

- 业务语义；
- 验收标准；
- public API / CLI / 文件格式等公共接口；
- Schema、Migration 和不可逆数据变化；
- 权限和安全规则；
- 隐私和数据保留；
- 外部服务费用或预算；
- 重大技术路线；
- 破坏性兼容变化。

例如：

```text
用户名重复时，是覆盖已有用户、创建新版本，还是直接报错？
```

这是业务规则，应由人确认。

如果 AI 反复询问能够自行调查的事实，可以直接说：

```text
能从当前仓库、代码、测试、CI 或工具确认的事实先自行确认。

只有真正需要业务、公共接口、数据、安全或重大技术路线决策时再问我。
```

---

## 6. 功能开发

### 6.1 直接开发

```text
基于当前仓库真实实现完成这个功能：<需求>。

先确认现有实现、调用链、公共接口和测试。

只修改真正需要修改的范围，不顺手重构无关代码。
不要擅自升级依赖、语言版本、框架或切换技术路线。

完成必要测试、文档同步和代码复核。
最终提交 PR 给维护者审核。
```

### 6.2 先讨论方案，再决定是否实施

当技术路线还没有确定时：

```text
先不要修改代码。

基于当前仓库真实实现讨论这个目标的实施方案：<目标>。

先确认当前架构、调用链、公共接口、Schema / 数据、依赖版本、测试和 CI。

区分：
- 已确认事实；
- 硬约束；
- 真正需要决定的问题；
- 暂时未知项。

有真实取舍时比较可行方案，并给出：
- 推荐方案；
- 主要依据；
- 影响范围；
- 兼容和 Migration；
- 回滚；
- 验证方式。

只输出方案，不修改代码。
```

方案确认后：

```text
按刚才已经确认的方案实施。

实施前重新核对当前仓库事实和方案前提。
仍然有效的已确认决定不要重复问我。

只有发现会改变业务、公共接口、数据、安全或重大技术路线的新冲突时，再说明问题并让我决定。

完成到 PR Ready。
```

### 6.3 已经从其他地方拿到方案，怎么落实到当前代码

其他聊天、同事、文档或工具给出的方案可能来自：

- 旧版本；
- 不同分支；
- 不同项目；
- 不完整上下文。

不要机械照搬。

可以说：

```text
这是从其他地方得到的方案：<方案 / 链接 / 文件>。

不要直接把它当成当前仓库事实。

先核对当前代码、接口、Schema / Migration、实际版本、测试和 CI。

把方案内容区分为：
1. 当前仍成立；
2. 已经过时，需要调整；
3. 与当前事实冲突；
4. 需要业务 / 公共接口 / 数据 / 安全 Owner 决策。

保持方案原本要解决的目标，再根据当前真实实现落地。
```

如果只想核对方案：

```text
只输出核对结果和调整后的实施方案，不修改代码。
```

### 6.4 复杂功能怎么拆

复杂功能不要默认拆成：

```text
数据库
→ 后端
→ 前端
→ 测试
```

这种方式经常会形成长时间互相等待。

优先按**可独立产生用户或调用者价值、并可独立验证的行为**拆分。

```text
把这个功能按可以独立产生用户 / 调用者价值，并且可以独立验证的纵向切片拆分：<需求>。

每个切片说明：
- 完整可观察行为；
- 验收方式；
- 真正的前置 blocker。

不要默认按数据库 / 后端 / 前端 / 测试水平拆分。
没有 blocker 的工作才可以并行。

先给拆分结果，不修改代码。
```

如果属于公共字段、共享类型等大范围机械迁移，无法安全纵向切分，则应根据当前项目情况采用分阶段兼容迁移，而不是强行套纵向切片。

### 6.5 任务很大、现在还看不清完整路径

不要要求 AI 在信息不足时一次性编造完整计划。

```text
这个任务很大，而且当前还看不清完整实施路径：<目标>。

先不要修改生产代码，也不要一次性编造完整任务列表。

先确认：
- 最终目标；
- 已确认决定；
- 当前可以立即调查或解决的问题；
- 被前置条件阻塞的问题；
- 当前还无法精确定义的问题；
- 非目标。

随着新的事实出现更新剩余问题。

等主要路径清晰以后，再形成正式实施计划和任务拆分。
```

### 6.6 只做架构或代码结构审视

```text
只做架构和代码结构审视，不修改代码：<范围>。

基于当前真实调用链检查：
- 同一业务能力和规则是否集中在合理位置；
- 调用者是否需要了解过多内部细节；
- 是否存在只转发参数的无价值中间层；
- 一次业务变化是否经常需要修改很多分散位置；
- 当前抽象是否对应真实变化点；
- 测试是否能够通过稳定入口证明行为。

只报告有当前证据支撑、确实会改善正确性或可维护性的高价值问题。

不要为了设计模式或“更优雅”建议无关重构。
```

---

## 7. Bug 修复

尽量告诉 AI **可观察症状**。

推荐：

```text
修复这个 Bug：<现象 / 复现步骤 / 报错>。

先复现或建立能够证明问题的失败证据，再定位根因。

如果根因会改变修法，不要为了最小 diff 提前收敛。

不要通过：
- 吞异常；
- 降低断言；
- 删除测试；
- 增加 timeout；
- 增加 retry；
- 增加临时兼容补丁；

来掩盖问题。

如果必须先止血，明确它只是临时缓解。

永久修复后验证：
- 原始症状；
- 关键原因 / 机制；
- 当前主要复发路径。

完成后提交 PR。
```

需要记住三个原则：

```text
分析深入
≠
最终改动一定很大
```

```text
最小充分
≠
最小代码改动
```

```text
症状暂时消失
≠
问题已经根治
```

如果已经有失败测试或 CI：

```text
根据当前失败测试 / CI 修复根因。

先读取完整失败输出和真实调用链，不要只针对报错行打补丁。
```

---

## 8. 重构和代码清理

```text
检查本次范围内的重复、失效和无用代码，并做必要清理。

保持当前合法行为、公共接口、配置、数据和运行方式不变。

只有能够证明没有真实消费者、调试价值、恢复价值或独立测试责任的内容才能删除。

不要以减少行数、文件数或测试数为目标。
不要顺手重构与当前范围无关的代码。

完成后运行与实际影响范围相匹配的验证，并说明主要删除 / 保留依据。
```

---

## 9. 独立功能测试 / 黑盒验收

如果你希望从用户视角独立判断功能是否正确：

```text
先不要修改生产代码。

针对当前功能做独立功能测试和黑盒验收。

从真实用户 / 调用者目标出发覆盖：
- 正常主路径；
- 关键边界；
- 错误处理；
- 失败恢复；
- 必要状态变化；
- 真实依赖边界。

不要把 Mock 结果冒充真实后端、数据库、外部服务或完整端到端结果。

发现确定缺陷时给出复现步骤、证据和影响。
```

更简单的表达：

```text
按真实用户场景验收这个页面，只测试，不改生产代码。
```

或者：

```text
对这个 Bug 做独立回归测试，同时检查相邻高风险场景。
```

---

## 10. Code Review

### 10.1 只 Review，不修改

```text
审查当前 PR / 分支，只做 Review，不修改代码。

先从真实需求和当前项目事实独立重建预期，再检查：
- 正确性；
- 边界条件；
- 错误处理；
- 安全和权限；
- 公共接口；
- 数据兼容；
- 测试是否真正证明目标行为；
- 文档是否与实现一致；
- 是否存在无关修改；
- 是否存在重复实现；
- 是否存在明显维护风险。

按严重程度列出 Findings。

没有证据的问题不要猜。
```

### 10.2 Review 后直接修复

如果明确希望 AI 修改代码：

```text
审查当前改动。

发现确定问题后直接修复，并完成必要回归和重新 Review。

不要修改与 Finding 无关的内容。

完成后更新当前 PR。
```

---

## 11. Figma / Design-to-Code / 前端开发

正式设计稿存在时，不要让 AI 根据截图或示例数据猜真实接口。

```text
按照这个正式 Figma 设计实现对应页面：<Figma 链接>。

同时检查：
- 当前正式设计和交互状态；
- 当前前端结构和公共组件；
- 后端真实 API / Contract / generated client；
- 动态数据；
- 日期；
- 权限；
- 错误状态；
- 空状态。

设计里的示例数据不能被当成真实接口。

如果设计和当前后端能力不一致，先指出差异，并根据真实系统能力判断怎么处理。

优先复用已有公共组件。
完成前端、接口和必要用户路径验证。
```

如果只是审查设计：

```text
全面检查这个 Figma：<链接>。

从以下角度审查：
- 布局；
- 信息层级；
- 交互状态；
- 公共组件复用；
- 开发可实现性；
- 与后端真实能力的对应关系。

只给 Findings，不修改代码。
```

---

## 12. 文档检查和同步

开发完成后，如果本次修改改变了长期项目事实，应同步真正受影响的文档。

可以直接说：

```text
检查本次修改是否影响长期技术文档。

重点检查：
- 接口；
- 数据和 Schema；
- 配置；
- 调用链；
- 构建和启动；
- 部署和回滚；
- 调试方式；
- 用户可见行为。

只更新真正受影响的文档。
没有事实变化就不要制造文档 diff。
```

如果只想审文档：

```text
检查当前技术文档是否与真实代码、接口、Schema、配置、测试和运行方式一致。

先界定受影响文档范围，不要机械扫描所有 Markdown。

发现实现问题时，不要通过修改文档把错误实现合法化。
```

---

## 13. 依赖、语言版本和技术方案升级

不要把依赖或运行环境升级夹在普通功能开发里顺手完成。

```text
评估并实施 <依赖 / 语言版本 / 框架> 版本升级。

先确认：
- 当前真实版本和 lock；
- 目标版本；
- 官方 / 一手资料；
- Breaking Changes；
- 兼容性；
- 安全影响；
- Migration；
- 回滚方式。

给出推荐后，再根据确认方案修改。

不要静默升级其他无关依赖。

完成与风险相匹配的回归验证。
```

---

## 14. Git 和团队协作

普通开发者推荐的默认方式：

```text
基于最新主分支完成这个任务。

使用独立任务分支开发。

完成实现、测试、文档和开发侧复核后提交 PR 给维护者审核。

不要合并主分支。
```

### 14.1 PR Review 发现问题以后怎么继续

一个正常的 PR 不一定第一次 Review 就直接合并。典型流程是：

```text
开发者提交 PR
    ↓
维护者 Review
    ↓
发现问题，提出 Review Findings / Request Changes
    ↓
原开发者根据 Review 意见继续修改
    ↓
更新原来的同一个 PR
    ↓
重新测试 / Review / CI
    ↓
维护者再次 Review
    ↓
没有阻塞问题后再按项目规则合并
```

这里通常**不需要关闭原 PR，也不需要重新创建一个 PR**。

原开发者继续修改当前 PR 对应的开发分支即可。新的修改进入这个分支后，会继续更新原来的 PR。

#### 原开发者收到 Review 意见后

直接告诉 AI：

```text
当前 PR 已收到维护者的 Review 意见。

读取当前 PR 的 Review Findings 和尚未解决的 Review comments。

逐条确认问题的真实原因，不要机械按照评论改代码。

对确认成立的问题：
- 只修改与 Finding 相关的范围；
- 不丢弃当前 PR 已有的正确实现；
- 不顺手扩大需求或进行无关重构；
- 补齐必要测试和文档；
- 对受影响范围重新进行验证和 Review。

修改完成后更新当前这个 PR。

不要新建第二个 PR。
不要合并主分支。

完成后告诉我：
- 每条 Review Finding 是怎么处理的；
- 修改了什么；
- 实际执行了哪些测试；
- Review 是否还有阻塞问题；
- 当前 PR / CI 状态；
- 是否还有未解决项。

如果已经满足重新审核条件，重新请求维护者 Review。
```

AI 应自行确认当前 PR、源分支、Review comments、已有修改和当前 HEAD，不需要用户手工处理 Git 命令。

#### 不要因为 Review 意见重新创建 PR

Review 后的修复仍然属于原 PR 的同一个交付单元时，应继续更新原 PR：

```text
原 PR
├── 初始开发提交
├── Review
├── Review 修复提交
├── 再 Review
├── 必要时继续修复
└── 最终通过后合并
```

不要变成：

```text
PR #1
→ Review 有问题
→ 关闭

PR #2
→ 修复第一次 Review

PR #3
→ 再修第二次 Review
```

这样会割裂 Review 上下文、CI、讨论记录和修改历史。

只有发现 Review 意见实际上属于**新的独立需求或明显超出当前 PR 范围**时，才应该单独建立新的工作单元，而不是静默扩大当前 PR。

#### 修改完成以后，维护者怎么重新审核

维护者可以直接告诉 AI：

```text
重新审查当前 PR。

这是上一轮 Review 后由原开发者修改过的版本。

先确认当前 PR 最新 HEAD，并重点检查：

- 上一轮 Review Findings 是否真正解决；
- 修复是否解决了根因，而不是只消除表面现象；
- 是否因为修复引入新的问题；
- 新增或修改的测试是否真正覆盖对应问题；
- 文档是否仍与实现一致；
- 是否出现无关修改或范围扩大；
- 当前 CI 是否对应最新 HEAD。

不要用上一轮 Review 或旧 CI 结果代替当前 revision 的验证。

如果仍有阻塞 Finding，继续提出修改意见并让原开发者更新同一个 PR。

如果已经没有阻塞 Finding，再按照当前项目的 Review、CI、权限和合并规则继续处理。
```

如果维护者上一轮使用了 `Request Changes`，修改代码本身不等于原来的审核意见已经自动通过。应基于修改后的最新 revision 再次 Review；项目存在 required review / branch protection 时，以当前实际 GitHub 门禁为准。

### 14.2 Review 返修的基本原则

Review 返修的目标是把当前需求修到可验收，不是把代码一直优化到“再也提不出意见”。AI 应**只修复阻塞当前目标**、有证据且属于当前范围的问题：

- **非阻塞** Finding 可以记录，但不因为它存在就反复修改当前 PR；
- **超出当前范围**的问题默认只记录；只有通过 Follow-up Admission Gate 才可进入后续 Backlog，而且不会自动执行或递归派生；
- 返修后重点复核原问题、本轮 diff、直接相邻风险和当前验收标准，不每轮重新无限扫描整个仓库；
- 如果连续返修没有明显收敛，或同一问题反复修不好，AI 应**停止机械返修**并**重新诊断**根因/方案，而不是继续“再试一次”；
- 自动返修轮次安全阀不是“到次数就通过”；仍有真正阻塞问题时必须保持未完成。

- **一个需求对应一个正常 PR 生命周期**，不要因为一次 Review 失败就重新创建 PR；
- Review Finding 必须逐条处理，不能只说“已经修改”；
- 修改后必须重新验证受影响范围；
- 旧测试、旧 CI、旧 Review 不能自动证明新的 HEAD；
- 修复 Finding 时不静默扩大当前需求；
- Review 发现新的独立需求时，单独建立后续工作；
- 原开发者负责把 PR 修到重新可审核状态；
- 维护者负责基于最新 revision 再次审核；
- 最终是否能够合并，以最新代码、Review、CI 和项目门禁为准。

### 14.3 已经有本地开发好的代码，但之前没有完整按当前项目要求开发

不需要为了补流程把代码重新写一遍。

可以说：

```text
当前分支已经有一批本地完成的开发改动，但之前的开发过程没有完全按照当前项目要求执行。

不要丢弃、覆盖或从头重写已有改动，也不要伪造过去没有执行过的流程。

把当前实现视为待验证的候选版本：

- 重新核对真实需求和当前项目事实；
- 检查实际 diff 和影响范围；
- 检查正确性、接口、数据、安全和兼容性；
- 补齐真正需要的测试和文档；
- 完成当前项目要求的 Review 和验证；
- 发现确定问题直接修复。

最终提交或更新 PR 给维护者审核，到 PR Ready 后停止。
```

目标是证明：

> **当前准备提交的 revision 已经获得足够的新鲜验证。**

而不是伪造：

> 过去开发时已经完整执行了所有流程。

### 14.4 Merge / Rebase 冲突

```text
当前 merge / rebase 发生冲突。

先恢复双方相关需求和提交意图，再逐个检查冲突块。

判断：
1. 两边行为是否可以同时保留；
2. 新需求是否已经明确取代旧行为；
3. 是否存在真正需要重新决定的业务 / 接口 / 数据 / 安全冲突。

不要机械选择 ours / theirs。

解决后运行受影响测试和回归。
```

---

## 15. 长任务和继续任务

对于跨多个阶段的大任务：

```text
继续当前大任务。

先恢复并核对：
- 最终目标；
- 已确认决定；
- 已完成部分及其证据；
- 当前 blocker；
- 已失败的假设；
- 未验证风险；
- 下一步；
- 非目标。

不要重复已经完成或已经被证伪的工作。

恢复后重新核对当前仓库 revision，再继续到原来的交付终点。
```

普通继续任务通常一句就够：

```text
继续当前任务，保持原来的完整范围和授权，完成到原来的交付终点。
```

如果只是继续开发到 PR：

```text
继续当前任务，完成到 PR Ready。
```

---

## 16. 怎么判断任务真的完成

不要只接受：

```text
已经完成。
```

或者：

```text
测试通过。
```

一个正式开发任务结束时，应至少能够说明与当前任务相关的：

- 改了什么；
- 为什么这样改；
- 关键能力发生了什么变化；
- 是否改变公共接口；
- 是否改变数据 / Schema；
- 是否改变依赖；
- 是否影响部署或回滚；
- 实际运行了哪些测试；
- 测试结果；
- Review 是否还有阻塞 Finding；
- 文档是否需要同步；
- 当前分支；
- commit；
- PR；
- CI；
- 哪些内容没有实际验证；
- 剩余风险。

如果报告不完整，可以直接说：

```text
不要只说完成。

按当前实际 revision 给我完整交付报告：

- 变更摘要；
- 实际测试和结果；
- Review；
- 文档影响；
- PR / CI 状态；
- 未验证项；
- 剩余风险。
```

---

## 17. 开发之外：分析问题和查资料

AI 也可以用于方案判断、问题分析和资料研究。

### 17.1 第一性原理分析

如果你问的是“还有没有优化”“全面系统排查”“是不是已经最好”，AI 不能因为当前 AC、测试或 CI 已通过就直接回答“没有问题”。它应从真实目标重新检查未覆盖前提、反例、二阶派生、生命周期、权限、并发、失败恢复、跨模型/宿主差异和成本；只能在明确的**已审计范围**内说明“没有发现更多高价值缺口”，并标明仍未验证的边界。

```text
用第一性原理分析这个问题：<问题>。

先检查我的前提是否成立，并区分：
- 已确认事实；
- 合理推断；
- 建议；
- 暂时无法验证。

先确认问题和必要根因 / 关键机制。

如果根因会改变方案，不要为了尽快给答案而提前收敛。

在稳定解决问题所需的必要条件内给最小充分方案。

最小充分不是最小改动，分析可以很深，而最终方案仍然可以很小。

如果需要立即控制风险，可以先给止血方案，但明确止血不等于根治。

不要为了显得完整给脱离现实的大而全设计。
```

### 17.2 查询当前最新资料

```text
查询当前最新资料后回答：<问题>。

优先官方、一手或直接拥有该事实的来源。

重要结论核对：
- 日期；
- 版本；
- 适用范围。

不要把搜索摘要或转载直接当成最终证据。

来源冲突或证据不足时明确说明。

证据已经足以回答问题后停止，不需要为了数量继续搜索。
```

### 17.3 研究历史资料

```text
只研究截至 2024-12-31 当时已经可以获得的资料回答：<问题>。

不要用后来发生的事实反推当时的结论。
```

简单翻译、短改写、算术和明确的简单问题直接提问即可。

---

## 18. 常用短指令速查

熟悉以后，大多数任务只需要下面这些短指令。

### 新功能

```text
基于当前仓库真实实现完成这个功能，完成必要测试和文档同步，最后提交 PR 给维护者审核。
```

### Bug

```text
修复这个 Bug，先复现并找根因，补必要回归测试，完成后提交 PR。
```

### 只讨论方案

```text
先不要改代码。

基于当前仓库真实实现讨论 <目标> 的方案，给推荐、取舍、影响、验证和回滚。
```

### 已确认方案继续实施

```text
按刚才确认的方案实施。

先重新核对当前仓库事实和方案前提，仍然有效的决定不要重复问我。
```

### 外部方案落地

```text
把这个外部方案落实到当前仓库：<方案>。

先逐项核对当前代码、Contract、Schema、版本、测试和 CI，再按当前真实实现调整和实施。
```

### 拆复杂任务

```text
把 <功能> 按可独立交付和验证的纵向切片拆分并列出 blocker。

不要默认按数据库 / 后端 / 前端 / 测试水平拆分。
```

### 大任务

```text
这个任务路径还不清楚。

先维护目标、已确认决定、当前可解决问题、blocker、未知项和非目标。

主要路径清晰后再形成正式实施计划。
```

### Review

```text
只审查当前 PR，不改代码。

从真实需求重新判断实现，并检查测试是否充分。
```

### Review 并修复

```text
审查当前 PR。

发现确定问题后直接修复，完成必要回归并重新 Review，然后更新当前 PR。
```

### PR Review 后返修

```text
根据当前 PR 的 Review Findings 修复问题，逐条核验并完成必要回归，然后更新同一个 PR，不要新建 PR，不要合并 main；完成后重新请求维护者 Review。
```

### 黑盒验收

```text
按真实用户场景对当前功能做黑盒验收，只测试，不修改生产代码。
```

### Figma

```text
按照这个正式 Figma 实现页面，并同时以后端当前真实 Contract 为准：<链接>。
```

### 文档

```text
检查本次修改真正影响的技术文档并同步当前事实，不要机械扫描全部 Markdown。
```

### 已有本地代码接管

```text
当前分支已有本地完成的修改，但之前没有完全按当前项目要求执行。

不要重做或覆盖已有工作。

把当前实现作为待验证候选版本，重新完成需求核对、影响检查、必要测试、文档和 Review；缺失的历史流程不要伪造。

最终提交 PR，到 PR Ready 后停止。
```

### 继续任务

```text
继续当前任务，保持原来的完整范围和授权，完成到原来的交付终点。
```

### Merge / Rebase 冲突

```text
当前 merge / rebase 有冲突。

先恢复双方需求和提交意图，再逐块解决。

不要机械选择 ours / theirs。

验证后只执行当前已授权的 Git 动作。
```

### 完整交付报告

```text
不要只说完成。

按当前 revision 给我完整交付报告：

变更摘要、实际测试、Review、文档影响、PR / CI 状态、未验证项和剩余风险。
```
