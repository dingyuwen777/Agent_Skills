# AI 辅助开发使用说明

本说明面向已经完成项目开发环境配置的普通开发者。你只需要在 **Codex、Cursor、Claude Code** 或团队允许的其他桌面 AI Agent 中打开项目，然后用自然语言描述任务。

你不需要了解或维护项目背后的治理实现，也不需要手工选择规则文件、内部能力或执行流程。AI 应先读取当前项目自己的规则和真实代码，再根据任务完成调查、实现、测试、文档、Review、Git/CI 和交付。

## 1. 日常使用原则

### 先让 AI 看当前项目，再改代码

不要把历史聊天、截图或印象当成当前实现。典型开场可以直接写：

```text
基于当前仓库真实实现处理这个任务。
先读取项目规则、相关代码、Contract、Schema/Migration、测试和 CI，再制定最小方案并执行。
能从仓库和工具确认的事实先自行确认，不要反问我。
```

AI 可以正常告诉你：正在检查哪些项目文件、发现了什么问题、准备怎么改、运行了哪些测试、文档是否需要同步、Review/CI/Git 当前是什么状态。

### 明确你希望任务做到哪里

同一个开发任务可以有不同交付终点。直接在需求里写清即可：

```text
只分析，不修改代码。
```

```text
完成修改和测试，但不要提交 Git。
```

```text
完成开发后提交 PR 给我审核，不要合并主分支。
```

```text
完成开发并合并到主分支，完成合并后的验证和收尾。
```

这些表达只代表你希望的结果，最终仍受当前仓库权限、Review、CI 和保护规则约束。

### 真正需要业务决策时再问人

普通实现细节、代码位置、版本、现有接口和测试入口应由 AI 自行调查。只有确实会改变业务语义、公共接口、Schema/数据、安全/权限、不可逆行为或重大技术路线，而且仓库没有既定决定时，才需要你确认。

## 2. 功能开发

适合新增或调整一个真实功能。

```text
基于当前仓库真实实现完成这个功能：<需求>。

要求：
1. 先确认现有实现、调用链、公共接口和测试；
2. 只做满足需求的最小修改，不顺手重构无关代码；
3. 不擅自升级依赖、Runtime、框架或切换技术方案；
4. 需要新增技术方案时先给推荐、理由和真实备选；
5. 完成必要测试、文档同步和代码复核；
6. 完成后提交 PR 给我审核，不要合并主分支。
```

如果你本人就是维护者，并且希望直接完成全部交付，可把最后一句改成：

```text
完成后合并到主分支，并完成主分支新鲜验证、适用的验收和收尾。
```

## 3. Bug 修复

不要只描述“哪里看起来不对”，尽量提供可观察症状。

```text
修复这个 Bug：<现象 / 复现步骤 / 报错>。

先复现或建立能证明问题的失败证据，再定位根因；
不要用吞异常、降低断言、删除测试或增加兼容补丁来掩盖问题。
修复后增加必要回归验证，并检查是否影响相邻调用链、文档或公共接口。
完成后提交 PR 给我审核，不要合并主分支。
```

如果已经有失败测试或 CI，可以直接让 AI 从该证据开始：

```text
根据当前失败的测试 / CI 修复根因。先读取完整失败输出和相关调用链，不要只针对报错行打补丁。
```

## 4. 重构与代码清理

适合删除无用代码、重复实现、整理结构或改善可维护性。

```text
全面检查本次范围内的重复、失效和无用代码，并做最小必要清理。

前提：
- 保持当前合法行为、公共接口、配置、数据和运行方式不变；
- 只有能证明没有真实消费者、调试价值、恢复价值或独立测试责任的内容才能删除；
- 不以减少行数、文件数或测试数为目标；
- 不顺手做与本任务无关的重构。

完成后运行与实际影响相称的验证，并说明删除/保留依据。
```

## 5. 独立功能测试 / 黑盒验收

当你不想先相信实现正确，而是想从用户视角验证功能时：

```text
先不要修改生产代码。
针对当前功能做独立功能测试和黑盒验收。

从真实用户 / 调用者目标出发设计关键场景，覆盖：
- 正常主路径；
- 关键边界；
- 错误和失败恢复；
- 必要的状态变化；
- 真实依赖边界。

不要把 Mock 结果冒充真实后端、数据库、外部服务或完整端到端结果。
发现确定缺陷时给出复现步骤、证据和影响，再决定是否进入修复。
```

也可以更简短：

```text
按真实用户场景验收这个页面，只做测试，不改代码。
```

```text
对这个 Bug 做独立回归测试，确认原问题和相邻高风险场景。
```

## 6. Code Review

### 只审查，不修改

```text
审查当前 PR / 分支改动，只做 Review，不修改代码。

先从真实需求和项目事实独立重建预期，再检查：
- 正确性和边界条件；
- 错误处理；
- 安全和权限；
- 公共接口、数据和兼容性；
- 测试是否真的证明目标行为；
- 文档是否与实现一致；
- 是否有无关改动、重复实现或维护风险。

按严重性列出 Findings；没有证据的问题不要猜。
```

### 审查并修复

只有你明确希望 AI 修改代码时再说：

```text
审查当前改动；发现确定问题后直接修复，并完成必要回归和重新 Review。
不要修改与 Finding 无关的代码。
完成后提交 PR 给我审核，不要合并主分支。
```

### 审查别人提交的 PR，通过后合并

维护者可以直接使用：

```text
审查 PR #<编号>。
先独立读取需求来源和实际 diff，不相信作者自述或 CI 绿色就等于需求完成。
如果存在阻塞问题，不要合并，明确列出 Findings。
如果 Review 通过，重新确认最新 head、required CI 和保护规则，然后合并到主分支并完成合并后的验证、验收和收尾。
```

## 7. 前端页面与 Figma / Design-to-Code

正式设计稿存在时，不要让 AI 根据截图或示例数据猜后端接口。

```text
按照这个正式 Figma 设计实现 / 替换对应页面：<Figma 链接>。

先同时检查：
- 当前正式设计和交互状态；
- 当前前端结构与公共组件；
- 后端真实 API / Contract / generated client；
- 动态数据、日期、权限、错误和空状态的真实来源。

设计中的示例数据不能被当成真实接口；
如果设计能力与后端当前能力不一致，先指出差异并以已确认的真实 Contract 为准。
优先复用已有公共组件，不为单个页面复制一套平行实现。
完成后做前端、接口和必要用户路径验证。
```

如果只是审设计，不开发：

```text
全面检查这个 Figma：<链接>。
从布局、层级、交互状态、公共组件复用、开发可实现性和后端真实能力映射角度审查；先只给 Findings，不修改代码。
```

## 8. 文档审查与同步

### 定向审查

```text
检查当前技术文档是否与真实代码、Contract、Schema、配置、测试和运行方式一致。
先界定受影响文档域，不要机械扫描所有 Markdown。
发现实现问题时不要通过修改文档把错误实现合法化。
```

### 开发后同步文档

```text
完成实现后检查这次变化是否改变长期项目事实、调用链、接口、数据、配置、运行/部署或调试方式。
只更新真正受影响的文档；没有事实变化就不要制造文档 diff。
```

## 9. 依赖、Runtime 或技术方案升级

升级类任务不要夹在普通功能开发中顺手做。

```text
评估并实施 <依赖 / Runtime / 框架> 升级。

先确认项目当前实际版本和 lock；
查与当前版本和目标版本匹配的一手资料；
比较收益、兼容性、Breaking Changes、安全、迁移和回滚；
给出推荐后再按已确认方案修改；
不得静默升级其他无关依赖。
完成与风险相匹配的完整回归，并说明迁移和回滚影响。
```

## 10. Git 与多人协作

### 普通开发者：提交 PR 给维护者审核

推荐明确写：

```text
基于最新主分支完成这个任务。
使用独立任务分支开发，完成实现、测试、文档和开发侧复核后提交 PR 给我审核。
不要合并主分支。
```

AI 到 PR Ready 后应停止，不因为自己能看到合并按钮就继续升级权限。

### 维护者：完整交付到主分支

```text
完成这个任务并合并到主分支。
不要把“merge API 成功”当成任务完成；还要完成当前项目要求的主分支新鲜验证、变更归档、验收状态同步、需求关闭和安全清理。
任一 required gate 未完成时如实报告 blocked / incomplete。
```

### 继续上一任务

如果前面已经明确范围和授权，可以直接说：

```text
继续这个任务，按原来的完整范围和交付目标完成。
```

AI 不应因为换了会话步骤或完成了一个子批次就把原任务静默缩小。

## 11. 不同 AI Agent 和模型

Codex、Cursor、Claude Code 等工具的界面和可用能力可能不同；它们使用的具体模型也可能不同。不要以“模型更强 / 更弱”作为降低工程标准的理由。

最终判断应依靠：

```text
当前项目真实事实
+ 当前实际 diff
+ 与风险相称的测试和验证
+ Review
+ CI / 分支保护
+ 实际交付结果回读
```

不同模型的推理过程、工具选择和写法可能不同，但进入主分支的代码必须满足同一个项目门禁。

## 12. 什么时候应该让 AI 停下来问你

以下事项如果仓库没有既定决定，通常需要你或业务 Owner 决策：

- 业务语义和验收标准；
- public API / CLI / 文件格式等公共 Contract；
- Schema、Migration 和不可逆数据行为；
- 权限、安全、隐私和数据保留；
- 外部 Provider、费用或预算；
- 重大技术路线或破坏性兼容变化。

下面这些通常不需要问你，应先自行调查：

- 代码在哪里；
- 当前依赖版本；
- 哪个测试命令存在；
- 当前接口实际长什么样；
- 某个文件是否被引用；
- 当前 CI 为什么失败；
- 项目已有的编码和文档约定。

如果 AI 反复询问可从仓库确认的事实，可以直接补一句：

```text
能从仓库、代码、测试、CI 或工具确认的事实先自行确认；只有真正需要业务 / Contract / 数据 / 安全决策的问题再问我。
```

## 13. 完成时应该看到什么

一个正式开发任务结束时，不应只得到“已完成”或“测试通过”。至少应能看到与任务实际范围相符的：

- 改了什么，以及为什么；
- 关键文件或能力变化；
- 是否改变公共接口、数据、依赖、部署或回滚；
- 实际执行过哪些测试 / 检查，结果是什么；
- 文档是否需要同步；
- Review 是否存在阻塞 Finding；
- 当前分支、提交、PR、CI 和合并状态；
- 哪些内容没有验证，以及剩余风险。

如果交付证据不完整，可以直接要求：

```text
不要只说完成了。请按当前实际 revision 给我完整交付报告：
变更摘要、实际测试命令和结果、Review、文档影响、PR/CI/合并状态、未验证项和剩余风险。
```

## 14. 常用短指令

日常使用不必每次写长提示词。下面这些短句通常已经足够明确：

```text
基于当前仓库真实实现完成这个功能，完成后提交 PR 给我审核，不要合并 main。
```

```text
修复这个 Bug，先复现并找根因，补回归测试，完成后提 PR。
```

```text
只审查 PR #123，不改代码；从真实需求重建预期并检查测试充分性。
```

```text
按真实用户场景做黑盒验收，只测试，不改生产代码。
```

```text
按照这个正式 Figma 实现对应页面，同时以后端真实 Contract 为准：<链接>。
```

```text
检查受影响技术文档并同步真实事实，不要机械扫描全部 Markdown。
```

```text
完成这个任务并合并到主分支，完成合并后全部适用收尾。
```