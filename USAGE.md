# AI 辅助开发使用说明

本说明面向已经完成项目开发环境配置的普通开发者。你只需要在 **Codex、Cursor、Claude Code、DeepSeek Harness** 或团队允许的其他桌面 AI Agent 中打开项目，然后用自然语言描述任务。

你不需要了解或维护项目如何配置这些开发约束，也不需要手工选择规则文件或执行流程。AI 应先读取当前项目自己的规则和真实代码，再根据任务完成调查、实现、测试、文档、Review、Git/CI 和交付。

> **DeepSeek Harness**：Windows 项目在完成维护者配置后，会在项目根提供 `DeepSeek-Harness.cmd`。日常使用时直接双击该文件即可，不需要手工输入较长的启动命令。

> **Windows 安装失败**：直接运行发布包中的 `.exe` 安装时，如果出现错误，窗口会保留错误信息并提示“按 Enter 键退出”，不会直接一闪而过。请按错误内容处理目标项目中的冲突后再重试，不要为了绕过错误强行覆盖项目已有文件；通过命令行脚本或其他非交互方式调用时仍会按失败退出码直接结束。

## 1. 日常使用原则

### 先让 AI 看当前项目，再改代码

不要把历史聊天、截图或印象当成当前实现。典型开场可以直接写：

```text
基于当前仓库真实实现处理这个任务。
先读取项目规则、相关代码、Contract、Schema/Migration、测试和 CI，再制定最小方案并执行。
能从仓库和工具确认的事实先自行确认，不要反问我。
```

AI 可以正常告诉你：正在检查哪些项目文件、发现了什么问题、准备怎么改、运行了哪些测试、文档是否需要同步、Review/CI/Git 当前是什么状态。

### 明确你希望任务做到哪里

普通开发者常用的交付终点有三种：

```text
只分析，不修改代码。
```

```text
完成修改和测试，但不要提交 Git。
```

```text
完成开发后提交 PR 给维护者审核，不要合并主分支。
```

这些表达只代表你希望的结果，最终仍受当前仓库权限、Review、CI 和保护规则约束。普通开发者完成 PR Ready 后应停止，由项目维护者负责最终审核和主分支合并。

### 真正需要业务决策时再问人

普通实现细节、代码位置、版本、现有接口和测试入口应由 AI 自行调查。只有确实会改变业务语义、公共接口、Schema/数据、安全/权限、不可逆行为或重大技术路线，而且仓库没有既定决定时，才需要你确认。

## DeepSeek Harness 特殊使用方式

### Windows

项目已经由维护者完成配置后，直接双击项目根的：

```text
DeepSeek-Harness.cmd
```

它会从当前项目打开 DeepSeek Harness。首次打开时，如果 Harness 要求项目信任或工具权限确认，请按团队安全策略完成确认。

前提是当前机器已经能够正常使用 `dsh` 命令。如果系统提示找不到 `dsh`，请先按团队环境说明修复 DeepSeek Harness 环境。

### Linux / macOS

项目完成配置后，从项目根按团队提供的 DeepSeek Harness 启动方式打开当前项目。当前平台没有 Windows 的 `.cmd` 双击入口。

### 排障

如果 DeepSeek Harness 无法打开当前项目：

1. 确认 `dsh` 命令可用；
2. 确认当前目录是正确的项目根；
3. 重新执行维护者提供的项目配置流程，或联系项目维护者处理受管配置。

不要自行编辑由项目配置流程生成的受管文件来绕过错误。

## 2. 功能开发

适合新增或调整一个真实功能。

```text
基于当前仓库真实实现完成这个功能：<需求>。

要求：
1. 先确认现有实现、调用链、公共接口和测试；
2. 只做满足需求的最小修改，不顺手重构无关代码；
3. 不擅自变更依赖、语言版本、框架或切换技术方案；
4. 需要新增技术方案时先给推荐、理由和真实备选；
5. 完成必要测试、文档同步和代码复核；
6. 完成后提交 PR 给维护者审核，不要合并主分支。
```

### 先讨论方案，再决定是否实施

如果你还没决定技术路线，可以先只讨论，不让 AI 修改代码：

```text
先不要修改代码。基于当前仓库真实实现讨论这个目标的实施方案：<目标>。

先确认当前架构、调用链、公共接口、Schema/数据、依赖版本、测试和 CI；
区分已确认事实、硬约束、真实待决策项和暂时未知项。
有实际取舍时比较可行方案，并重点检查职责是否集中、接口是否过宽、是否存在无意义转发/重复实现，以及一次业务变化是否被迫散落到太多位置。
给出推荐方案、主要依据、影响范围、兼容/迁移/回滚和验证方式。
只输出可实施方案，不修改代码、不提交 Git。
```

方案确认后，可以直接继续：

```text
按刚才已经确认的方案实施。
实施前重新核对当前仓库事实和方案前提；仍然有效的已确认决定不要重复问我。
如果发现会改变业务语义、公共接口、Schema/数据、安全/权限或重大技术路线的新事实，再说明冲突和推荐方案让我决定；其他可核实实现细节自行处理。
按当前项目规则完成最小实现、必要测试、文档同步和 Review，最后提交 PR 给维护者审核，不要合并主分支。
```

### 已经从其他地方拿到方案，怎么落实到当前代码

其他聊天、同事、文档或工具给出的方案可能基于旧版本或不同项目状态，不应机械照搬，也不需要无理由全部推翻。可以直接说：

```text
这是从其他聊天 / 同事 / 文档 / 工具得到的方案：<粘贴方案、文件路径或链接>。

不要把它直接当成当前仓库事实。
先读取当前项目规则、代码、Contract、Schema/Migration、实际版本、测试和 CI，逐项判断方案内容：
1. 当前仍成立；
2. 已经过时，需要按当前实现调整；
3. 与当前事实冲突；
4. 只有业务 / 公共接口 / 数据 / 安全 / 重大路线 Owner 才能决定。

保持原方案要解决的目标，不要为了实现细节差异把整个方案推翻；也不要为了照搬方案去修改正确的当前事实。
把仍适用的方案映射到当前真实模块和调用链后实施；只有真正需要我决策的冲突再问我。
完成必要验证、文档和 Review，最后提交 PR 给维护者审核，不要合并主分支。
```

如果你只想先核对方案是否还能用，把最后一句改成“**只输出核对结果和调整后的实施方案，不修改代码**”。

### 复杂功能怎么拆

复杂功能不要默认按“数据库 / 后端 / 前端 / 测试”分成互相等待的长链。可以要求按可独立产生结果的纵向切片拆：

```text
先把这个功能按可以独立产生用户 / 调用者价值并独立验证的纵向切片拆分：<需求>。

每个切片说明它交付的完整可观察行为、验收方式和真正的前置 blocker；
不要默认按数据库、后端、前端、测试水平拆分。
没有 blocker 的工作才可以并行。
如果属于共享字段、公共类型等大范围机械迁移，无法安全纵向切片，就改用“先兼容扩展 → 分批迁移消费者 → 最后删除旧形式”，并说明每批的验证和依赖。
先给拆分结果，不修改代码。
```

确认拆分后，再让 AI 按依赖顺序实现到你要求的交付终点。

### 任务很大、现在还看不清完整路径

对于明显超过一个会话、当前还无法可靠列出完整实施步骤的大任务，不要强迫 AI 一开始假装拥有完整计划：

```text
这个任务很大，而且当前还看不清完整实施路径：<目标>。

先不要修改生产代码，也不要一次性编造完整任务清单。
先明确最终目标、已经确认的决定、当前可以立即调查/决策的问题、被前置条件阻塞的问题、暂时还无法精确定义的问题，以及明确的非目标。
逐步解决当前已经足够明确的问题；每得到新事实就更新剩余问题。
等主要路径真正清晰后，再给正式实施方案和任务拆分。
```

### 只做架构 / 代码结构审视

```text
只做架构和代码结构审视，不修改代码：<范围>。

基于当前真实调用链检查：
- 同一业务能力和规则是否集中在正确位置；
- 调用者是否需要了解过多内部细节；
- 是否存在只转发参数的无价值中间层；
- 一次业务变化是否经常要同时修改很多分散位置；
- 当前抽象是否对应真实变化点，而不是为假想未来设计；
- 测试是否能通过稳定入口证明行为。

只报告有当前证据支撑、确实会改善正确性或可维护性的高价值问题；不要为了设计模式或“更优雅”建议无关重构。
```

## 3. Bug 修复

不要只描述“哪里看起来不对”，尽量提供可观察症状。

```text
修复这个 Bug：<现象 / 复现步骤 / 报错>。

先复现或建立能证明问题的失败证据，再定位根因；
不要用吞异常、降低断言、删除测试或增加兼容补丁来掩盖问题。
修复后增加必要回归验证，并检查是否影响相邻调用链、文档或公共接口。
完成后提交 PR 给维护者审核，不要合并主分支。
```

如果已经有失败测试或 CI，可以直接让 AI 从该证据开始：

```text
根据当前失败的测试 / CI 修复根因。先读取完整失败输出和相关调用链，不要只针对报错行打补丁。
```

## 4. 重构与代码清理

适合删除无用代码、重复实现、整理结构或改善可维护性。

```text
全面检查本次范围内的重复、失效和无用代码，并做最小必要清理。

前提：
- 保持当前合法行为、公共接口、配置、数据和运行方式不变；
- 只有能证明没有真实消费者、调试价值、恢复价值或独立测试责任的内容才能删除；
- 不以减少行数、文件数或测试数为目标；
- 不顺手做与本任务无关的重构。

完成后运行与实际影响相称的验证，并说明删除/保留依据。
完成后提交 PR 给维护者审核，不要合并主分支。
```

## 5. 独立功能测试 / 黑盒验收

当你不想先相信实现正确，而是想从用户视角验证功能时：

```text
先不要修改生产代码。
针对当前功能做独立功能测试和黑盒验收。

从真实用户 / 调用者目标出发设计关键场景，覆盖：
- 正常主路径；
- 关键边界；
- 错误和失败恢复；
- 必要的状态变化；
- 真实依赖边界。

不要把 Mock 结果冒充真实后端、数据库、外部服务或完整端到端结果。
发现确定缺陷时给出复现步骤、证据和影响，再决定是否进入修复。
```

也可以更简短：

```text
按真实用户场景验收这个页面，只做测试，不改代码。
```

```text
对这个 Bug 做独立回归测试，确认原问题和相邻高风险场景。
```

## 6. Code Review

### 只审查，不修改

```text
审查当前 PR / 分支改动，只做 Review，不修改代码。

先从真实需求和项目事实独立重建预期，再检查：
- 正确性和边界条件；
- 错误处理；
- 安全和权限；
- 公共接口、数据和兼容性；
- 测试是否真的证明目标行为；
- 文档是否与实现一致；
- 是否有无关改动、重复实现或维护风险。

按严重性列出 Findings；没有证据的问题不要猜。
```

### 审查并修复

只有你明确希望 AI 修改代码时再说：

```text
审查当前改动；发现确定问题后直接修复，并完成必要回归和重新 Review。
不要修改与 Finding 无关的代码。
完成后提交 PR 给维护者审核，不要合并主分支。
```

普通开发者的 Review 可以发现问题、补充证据或修复自己负责的分支，但不因此获得主分支合并权限。

## 7. 前端页面与 Figma / Design-to-Code

正式设计稿存在时，不要让 AI 根据截图或示例数据猜后端接口。

```text
按照这个正式 Figma 设计实现 / 替换对应页面：<Figma 链接>。

先同时检查：
- 当前正式设计和交互状态；
- 当前前端结构与公共组件；
- 后端真实 API / Contract / generated client；
- 动态数据、日期、权限、错误和空状态的真实来源。

设计中的示例数据不能被当成真实接口；
如果设计能力与后端当前能力不一致，先指出差异并以已确认的真实 Contract 为准。
优先复用已有公共组件，不为单个页面复制一套平行实现。
完成后做前端、接口和必要用户路径验证。
完成后提交 PR 给维护者审核，不要合并主分支。
```

如果只是审设计，不开发：

```text
全面检查这个 Figma：<链接>。
从布局、层级、交互状态、公共组件复用、开发可实现性和后端真实能力映射角度审查；先只给 Findings，不修改代码。
```

## 8. 文档审查与同步

### 定向审查

```text
检查当前技术文档是否与真实代码、Contract、Schema、配置、测试和运行方式一致。
先界定受影响文档域，不要机械扫描所有 Markdown。
发现实现问题时不要通过修改文档把错误实现合法化。
```

### 开发后同步文档

```text
完成实现后检查这次变化是否改变长期项目事实、调用链、接口、数据、配置、运行/部署或调试方式。
只更新真正受影响的文档；没有事实变化就不要制造文档 diff。
```

## 9. 依赖、语言版本或技术方案变更

版本和技术方案变更不要夹在普通功能开发中顺手做。

```text
评估并实施 <依赖 / 语言版本 / 框架> 版本变更。

先确认项目当前实际版本和 lock；
查与当前版本和目标版本匹配的一手资料；
比较收益、兼容性、Breaking Changes、安全、迁移和恢复方案；
给出推荐后再按已确认方案修改；
不得静默变更其他无关依赖。
完成与风险相匹配的完整回归，并说明迁移和恢复影响。
完成后提交 PR 给维护者审核，不要合并主分支。
```

## 10. Git 与多人协作

普通开发者推荐始终明确：

```text
基于最新主分支完成这个任务。
使用独立任务分支开发，完成实现、测试、文档和开发侧复核后提交 PR 给维护者审核。
不要合并主分支。
```

到 PR Ready 后停止。即使桌面 AI Agent 当前技术上能够调用更多 Git 操作，也不能自行扩大到主分支合并、Release、部署或生产数据操作。

如果维护者 Review 提出修改意见，可以继续：

```text
根据当前 PR 的 Review Findings 修复问题。
先确认每条 Finding 的真实原因，只修改相关范围；
完成回归和重新自检后更新同一个 PR，仍然不要合并主分支。
```

### Merge / Rebase 冲突

已经处于 merge / rebase 且发生冲突时，可以直接说：

```text
当前正在 merge / rebase 并发生冲突。

先确认当前 Git 操作状态、base/current/另一侧 revision 和全部冲突文件；
再追踪双方相关 commit、PR、Issue / 需求来源和代码，理解双方各自要保留的真实行为。
逐个冲突块判断是：
1. 两边意图可以兼容并保留；
2. 当前正式要求已经明确取代旧行为；
3. 两边存在真正的业务 / 接口 / 数据 / 安全设计冲突，需要重新决策。

不要机械选择 ours / theirs，也不要默认 abort。
解决后运行受影响的测试和回归；只有当前授权允许时才继续 stage、rebase/merge continue、commit、push 或后续合并。
```

### 已经在本地完成开发，但之前没有完全按当前项目开发规则执行

如果代码已经在本地开发完成，但开发过程中没有从一开始完整按照当前项目的开发、测试、Review 或变更治理规则执行，**不要直接把现有代码推到远程，也不需要为了形式重新开发一遍**。

这时应让 AI 接管当前已有改动，把它视为“尚未完成交付验证的候选实现”，按照当前仓库真实规则重新完成 PR 前真正需要的检查、验证和修复。

可以直接说：

```text
当前分支已经有一批本地完成的开发改动，但之前的开发过程没有完全按照当前项目开发规则执行。

请不要丢弃、覆盖或从头重写这些已有改动，也不要假装过去已经执行过不存在的 TDD、Change、Issue、Review 或测试。

请基于当前仓库和当前实际工作区，按照当前项目开发规则接管这批已有实现，并完成到 PR Ready：

1. 先检查当前 branch、base、Git 状态、已有 commits、未提交修改和完整 diff，保护现有工作，不使用 reset --hard、clean、force push 等破坏性操作；
2. 重新读取当前项目规则、真实 Requirement Source、相关代码和调用链，从上游事实独立重建本次改动应该满足的目标、范围、非目标和验收标准，不从当前代码反推需求；
3. 根据实际 diff 和影响重新判断风险等级，以及当前真正需要的 Change、Requirement Traceability、测试、文档和其他治理门禁；
4. 如果以前缺少某些流程证据，不要伪造历史。当前规则要求建立治理记录时按真实现状建立，并明确这是对既有实现的接管和补充验证；
5. 把当前已有实现视为未经独立验证的候选实现，系统检查正确性、边界条件、错误处理、安全、权限、公共 Contract、Schema/数据、兼容性、依赖、无关改动和维护风险；
6. 检查现有测试是否真正覆盖本次行为。缺少必要回归证据时补最小充分测试，并运行与实际风险相称的验证；
7. 对 Bug 修复或行为变化，如果能够在不破坏现有工作、不重写共享历史且环境仍可复现的前提下，在修改前 base/revision 上验证失败、在当前实现上验证通过，可以记录为 base Red → current Green 的事后回归证据；但不要把这种事后验证描述成开发时已经执行过 TDD；
8. 按当前仓库门禁完成独立 Review；真实用户场景、功能测试或其他专项验证按实际风险和当前规则决定是否需要；发现确定问题直接在当前任务范围内修复，然后重新验证；
9. 检查并同步真正受影响的技术文档；没有事实变化时不要制造文档 diff；
10. 检查当前分支相对目标主分支是否过期或存在冲突，按照仓库既有策略安全处理，不覆盖已有工作、不默认 rebase 或重写共享历史；
11. 所有必要检查和开发侧 Review 通过后，提交并推送当前任务分支，创建或更新 PR 给维护者审核；
12. PR 中如实说明这是已有本地实现经过当前规则接管、补充审查和验证后的提交，并列出实际验证证据、未验证项和剩余风险；不要声称开发从一开始就完整遵循了当前规则；
13. 到 PR Ready 后停止，不要自行合并主分支。

不要为了补流程机械创建 Issue、Change、测试或其他记录。只有当前仓库规则、本次风险和真实交付门禁要求时才建立；已经缺失的历史过程只能如实说明，不能倒填或伪造。
```

这个流程的目标不是证明“过去的开发过程已经符合当前项目规则”，而是证明当前准备提交 PR 的 revision 已经按现行规则取得足够、可追溯的新鲜证据。

### 继续上一任务

如果前面已经明确范围和授权，可以直接说：

```text
继续这个任务，按原来的完整范围完成到 PR Ready。
```

AI 不应因为换了会话步骤或完成了一个子批次就把原任务静默缩小。

对于跨多阶段的大任务，项目治理能力会把“规则上下文”和“问题求解状态”分开：目标、成功标准、已确认决定、已完成切片及 Evidence、当前前沿、阻塞、失败假设、未验证风险、下一步和非目标只在阶段实质变化、上下文压缩、任务交接或宿主重连前更新。正常使用时你不需要维护隐藏状态文件；如果宿主能够保留上一 checkpoint 的状态，重连后应从它恢复。如果宿主或新会话没有保留该状态，就必须重新从仓库、Issue/Change/PR 和实际 Evidence 恢复事实，不能假装自动记住。

## 11. 不同 AI Agent 和模型

Codex、Cursor、Claude Code、DeepSeek Harness 等工具的界面和可用能力可能不同；它们使用的具体模型也可能不同。不要以“模型更强 / 更弱”作为降低工程标准的理由，也不需要为 GPT、DeepSeek、GLM 等模型手工选择不同的开发规则集。

相同项目事实、任务范围和授权应使用同一工程 Contract。模型可以采用不同的推理方式、调查次数和工具顺序，但不能改变公共接口、风险等级、权限、测试/Review/CI 门禁或“什么证据才算完成”。

如果团队要比较不同模型的实际使用效果，应让它们执行同一组真实任务并按同一 Outcome Eval 标准记录证据。某个模型没有实际运行证据时只能说“尚未验证”，不能因为另一个模型通过、模型厂商宣传或静态 Skill 测试通过就推断效果相同。

最终判断应依靠：

```text
当前项目真实事实
+ 当前实际 diff
+ 与风险相称的测试和验证
+ Review
+ CI / 分支保护
+ 实际交付结果回读
```

不同模型的推理过程、工具选择和写法可能不同，但能够进入主分支的代码必须满足同一个项目门禁。

## 12. 什么时候应该让 AI 停下来问你

以下事项如果仓库没有既定决定，通常需要你或业务 Owner 决策：

- 业务语义和验收标准；
- public API / CLI / 文件格式等公共 Contract；
- Schema、Migration 和不可逆数据行为；
- 权限、安全、隐私和数据保留；
- 外部 Provider、费用或预算；
- 重大技术路线或破坏性兼容变化。

下面这些通常不需要问你，应先自行调查：

- 代码在哪里；
- 当前依赖版本；
- 哪个测试命令存在；
- 当前接口实际长什么样；
- 某个文件是否被引用；
- 当前 CI 为什么失败；
- 项目已有的编码和文档约定。

如果 AI 反复询问可从仓库确认的事实，可以直接补一句：

```text
能从仓库、代码、测试、CI 或工具确认的事实先自行确认；只有真正需要业务 / Contract / 数据 / 安全决策的问题再问我。
```

## 13. 完成时应该看到什么

一个正式开发任务结束时，不应只得到“已完成”或“测试通过”。至少应能看到与任务实际范围相符的：

- 改了什么，以及为什么；
- 关键文件或能力变化；
- 是否改变公共接口、数据、依赖、部署或恢复；
- 实际执行过哪些测试 / 检查，结果是什么；
- 文档是否需要同步；
- Review 是否存在阻塞 Finding；
- 当前分支、提交、PR 和 CI 状态；
- 哪些内容没有验证，以及剩余风险。

如果交付证据不完整，可以直接要求：

```text
不要只说完成了。请按当前实际 revision 给我完整交付报告：
变更摘要、实际测试命令和结果、Review、文档影响、PR/CI 状态、未验证项和剩余风险。
```

## 14. 常用短指令

日常使用不必每次写长提示词。下面这些短句通常已经足够明确：

```text
基于当前仓库真实实现完成这个功能，完成后提交 PR 给维护者审核，不要合并 main。
```

```text
修复这个 Bug，先复现并找根因，补回归测试，完成后提 PR。
```

```text
只审查 PR #123，不改代码；从真实需求重建预期并检查测试充分性。
```

```text
按真实用户场景做黑盒验收，只测试，不改生产代码。
```

```text
按照这个正式 Figma 实现对应页面，同时以后端真实 Contract 为准：<链接>。
```

```text
检查受影响技术文档并同步真实事实，不要机械扫描全部 Markdown。
```

```text
继续当前任务，完成到 PR Ready，交给维护者审核，不要合并主分支。
```

```text
当前分支已有本地开发完成的改动，但之前没有完全按当前项目开发规则执行。请不要重做或覆盖现有工作，把当前实现作为待验证候选版本，按当前仓库真实规则重新完成需求核对、风险判断、必要测试、文档、独立 Review 和 Git 交付；缺失的历史流程不要伪造。发现问题直接修复，最终提交 PR 给维护者审核，到 PR Ready 后停止，不要合并 main。
```

```text
先不要改代码；基于当前仓库真实实现讨论 <目标> 的方案，给推荐、取舍、影响、验证和回滚，等我确认后再实施。
```

```text
按刚才确认的方案实施；先重新核对当前仓库事实和方案前提，只有出现会改变业务/公共接口/数据/安全/重大路线的新冲突时再问我。
```

```text
把这个外部方案落实到当前仓库：<方案>。先逐项核对当前代码、Contract、Schema、版本、测试和 CI，标出仍成立/已过时/冲突/需决策，再按当前真实实现落地。
```

```text
把 <功能> 按可独立交付和验证的纵向切片拆分并列出 blocker；不要默认按数据库/后端/前端/测试水平拆。
```

```text
这个任务太大且路径还不清楚；先维护最终目标、已确认决定、当前可解决问题、被阻塞项、暂时无法精确定义的问题和非目标，路径清晰后再形成正式实施计划。
```

```text
当前 merge / rebase 有冲突；先恢复双方需求和提交意图，再逐块解决，不机械选 ours/theirs；验证后只执行当前已授权的 Git 动作。
```
