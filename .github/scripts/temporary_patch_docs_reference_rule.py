from pathlib import Path


ROOT = Path(__file__).resolve().parents[2]


def replace_once(path: Path, old: str, new: str) -> None:
    """只允许唯一命中后替换，避免补丁因上游内容漂移而误写。"""
    text = path.read_text(encoding="utf-8")
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{path.relative_to(ROOT)} 目标片段命中 {count} 次，停止修改")
    path.write_text(text.replace(old, new), encoding="utf-8")


skill = ROOT / ".agents/skills/docs/SKILL.md"
writing = ROOT / ".agents/skills/docs/references/02_第一性原理技术写作.md"
workflow = ROOT / ".agents/skills/docs/references/03_审查编写与修复流程.md"

replace_once(
    skill,
    "13. **以实用性为结束条件。** 读者看完应知道为什么、怎么流、去哪里找真实实现、怎么验证或排障。文档没有帮助读者完成真实任务时，即使术语很多、章节很多也不算高质量。\n\n## 2. Docs Impact：让同步有效但不变重",
    "13. **以实用性为结束条件。** 读者看完应知道为什么、怎么流、去哪里找真实实现、怎么验证或排障。文档没有帮助读者完成真实任务时，即使术语很多、章节很多也不算高质量。\n14. **仓库内具体文档引用必须同时可定位、可点击。** 当 Markdown 文档引用当前仓库内另一个具体文档，且目标可由当前仓库事实确认时，统一使用**完整仓库相对路径 + 可点击链接**：显示文字保留完整仓库相对路径，链接目标使用从当前文档位置可解析的相对路径并验证真实存在。不得只写不可点击的 inline-code 路径，也不得用“这里”“详情”等隐藏真实目标；命令、目录树、glob、占位路径、协议/流程示例、生成路径等非导航内容不机械链接化。模板或生成型文档必须按最终输出位置重新验证相对链接。详细写法见 [02_第一性原理技术写作.md](references/02_第一性原理技术写作.md)，审查与修复见 [03_审查编写与修复流程.md](references/03_审查编写与修复流程.md)。\n\n## 2. Docs Impact：让同步有效但不变重",
)

replace_once(
    skill,
    "代码/Contract/Schema 等引用真实且有理解价值\n术语首次出现已有白话解释",
    "代码/Contract/Schema 等引用真实且有理解价值\n仓库内具体文档引用显示完整仓库相对路径并可点击，且链接已从当前文档或最终生成位置验证\n术语首次出现已有白话解释",
)

replace_once(
    writing,
    "类名、函数名、表名、接口名同理。\n\n## 6. 不同文档有不同读者任务",
    """类名、函数名、表名、接口名同理。

### 5.1 仓库内具体文档引用：路径可见且链接可点

当 Markdown 技术文档引用**当前仓库内另一个具体文档**，并且目标文件已经由当前仓库事实确认时，导航必须同时满足“人能看清目标”和“可以直接打开”：

- **link label 使用完整仓库相对路径。** 例如目标事实是 `docs/architecture.md`，显示文字就保留 `docs/architecture.md`，不要只写“架构文档”“这里”“详情”这类隐藏真实位置的标题；
- **link target 使用从当前文档位置可解析的相对路径。** 不为了方便绑定 GitHub 域名、固定 branch 或本机绝对路径；目标必须真实存在，不能为了生成链接猜一个可能的文件名；
- **不得只写不可点击的 inline-code 路径。** 如果这段文字的职责就是让读者去读另一个真实文档，应提供链接，而不是只给 `` `docs/architecture.md` `` 让读者复制查找；纯文档导航也不应仅用 fenced code block 承担；
- **模板和生成型文档按最终位置验证。** 模板源文件所在目录与最终生成文档所在目录可能不同；相对链接必须在**最终输出位置重新验证**，不能只证明模板源码位置能点；
- **目标缺失时失败关闭。** 路径不存在、文档已改名或当前事实不足时，先恢复正确目标；无法确认就明确报告断链/未确认，不创建看似可点但实际错误的链接；
- **不要机械链接化非导航内容。** 命令、目录树、glob、`<placeholder>`、协议/流程示例、生成路径、代码字面量即使看起来像文件名，也只有在语义上确实承担“打开当前仓库真实文档”的导航职责时才转成链接；
- 外部网站/外部文档不属于“仓库相对路径”规则；使用已经确认的稳定 URL 和对读者有意义的链接文字，不伪造仓库路径。

这条规则的目的不是追求 Markdown 形式统一，而是同时保证**可定位性、可点击性、可移植性和事实正确性**。

## 6. 不同文档有不同读者任务""",
)

replace_once(
    workflow,
    "- 代码路径/命令/链接已失效；\n- 文档声称的能力尚未实现；",
    "- 代码路径/命令/链接已失效；\n- 仓库内具体文档引用没有显示完整仓库相对路径、只有不可点击路径、链接 target 已失效，或误把命令、目录树、glob、占位路径等非导航内容机械链接化；\n- 文档声称的能力尚未实现；",
)

replace_once(
    workflow,
    "哪些机器事实只应该链接/定位，不能复制？\n```",
    "哪些机器事实只应该链接/定位，不能复制？\n哪些仓库内具体文档引用需要同时保留完整路径和可点击链接？\n```",
)

replace_once(
    workflow,
    "- 两份 Markdown 同时维护同一精确规则。\n\n如果有，优先保留解释和链接真实事实源。",
    "- 两份 Markdown 同时维护同一精确规则；\n- 仓库内具体文档链接的显示路径、相对 target 或最终生成位置已经与真实文件漂移。\n\n如果有，优先保留解释和链接真实事实源；链接类问题按当前文档/最终输出位置恢复真实相对目标，不用不可点击路径或猜测性链接掩盖。",
)
